<?php
 
Core::model('db', 'core')->updateCount('members', 'coins', 1, ('+' . 1, 'member_id');
 
// read the post from PayPal system and add 'cmd'
$req = 'cmd=_notify-validate';
foreach ($_POST as $key => $value) {
$value = urlencode(stripslashes($value));

